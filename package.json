{
  "name": "lab-satisfying-lasers",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Lab for laser/beam FX. Dev-tooling manifest only: the served site has zero runtime dependencies and needs no install.",
  "scripts": {
    "serve": "python3 -m http.server 8081 --bind 127.0.0.1",
    "test": "node --test tests/*.test.js"
  }
}
